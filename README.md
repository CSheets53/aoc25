# Advent of Code 2025
File structure is setup to where each day's folder expects the input as a file named `dayX.txt`. For example, running `day1.py` expects a file in the `day1/` folder named `day1.txt`.
